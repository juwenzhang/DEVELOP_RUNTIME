# Cpp 数据类型

:::note

* 整形

* 浮点型

* 字符型 `''`

* 数组类型 `[]`

* 字符串 `""`

* 指针类型 `*`

* 引用类型 `&`

* 结构体类型 `struct`

* 向量类型 `vector`

* 类 `class`

* 栈 `stack`

* 队列 `queue`

* 双端队列 `deque`

* 哈希表 `unordered_map[hash_table]`

* 映射 `map`

* 集合 `Set`

:::