{
  "name": "develop_runtime",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "docs:dev": "rspress dev",
    "docs:build": "rspress build",
    "docs:preview": "rspress preview",
    "prepare": "husky",
    "check:build": "pnpm run -r build",
    "check:commitlint": "echo 'Commitlint配置已存在，通过GitHub Actions验证'",
    "check:release-config": "echo 'Semantic-release配置已存在，通过GitHub Actions验证 (需要Node.js >=20.8.1)'",
    "check:all": "pnpm run check:build && pnpm run check:commitlint && pnpm run check:release-config && pnpm run check:workflows",
    "check:workflows": "node ./scripts/validate-workflows.js"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "echo 'Linting files...'"
    ],
    "*.{md,json,yaml,yml}": [
      "echo 'Checking formatting...'"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@10.14.0",
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "@dotenvx/dotenvx": "^1.51.0",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/commit-analyzer": "^13.0.1",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/npm": "^12.0.2",
    "@shikijs/transformers": "^3.13.0",
    "@types/node": "18.11.17",
    "actionlint": "^2.0.6",
    "cz-conventional-changelog": "^3.3.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.3",
    "neon-cli": "^0.10.1",
    "semantic-release": "^24.2.9",
    "shiki-transformers": "link:@rspress/core/shiki-transformers"
  },
  "dependencies": {
    "rspress": "^1.45.6"
  }
}
